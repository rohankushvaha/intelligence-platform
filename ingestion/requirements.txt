# LIP v2 — Python Ingestion Pipeline Dependencies
# Install: pip install -r requirements.txt
#
# Minimum Python version: 3.11
# ─────────────────────────────────────────────────────────────────────────

# Web crawling — Firecrawl handles JS rendering, bot detection, robots.txt
firecrawl-py==1.9.0

# LLM orchestration + text splitting
langchain==0.3.25
langchain-community==0.3.23

# Observability — LangSmith free tier: 5,000 traces/month
langsmith==0.3.45

# Embeddings — same model as v1 (384 dims, ~90MB)
sentence-transformers==3.4.1

# Supabase client — pgvector upsert + RPC calls
supabase==2.15.1

# Environment variable management
python-dotenv==1.1.0

# Token counting for chunk size validation
tiktoken==0.9.0

# PDF text extraction — pypdf replaces deprecated PyPDF2
pypdf==5.4.0

# Sentiment analysis — runs locally, no API cost
transformers==4.51.3

# CPU-only PyTorch — sufficient for inference, much smaller download than CUDA
# If on Apple Silicon, pip will auto-select the MPS-compatible build
torch==2.7.0

# Progress bars for embedding batches
tqdm==4.67.1

# HTTP client used internally by several packages — pin for stability
httpx==0.28.1
